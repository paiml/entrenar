{
  "name": "entrenar-monitor-demo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:wasm": "cd .. && wasm-pack build --target web --out-dir wasm-pkg/pkg --features monitor",
    "serve": "npx serve . -p 9877",
    "test:e2e": "npx playwright test",
    "test:e2e:ui": "npx playwright test --ui",
    "test:e2e:headed": "npx playwright test --headed",
    "test:e2e:update": "npx playwright test --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "serve": "^14.0.0"
  }
}
