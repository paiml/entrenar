model:
  path: "model.gguf"
  layers: []

data:
  train: "data/train.parquet"
  batch_size: 32

optimizer:
  name: "adam"
  lr: 0.0001

training:
  epochs: 1
  lr_scheduler: cosine
  warmup_steps: 10
