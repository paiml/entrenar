# YAML-056: Config Validation (Poka-Yoke)
# QA Focus: Invalid types caught immediately
# Validation: 25-point checklist required

model:
  path: ./models/mock.gguf
  layers: []

data:
  train: ./data/train.parquet
  batch_size: 32

optimizer:
  name: adam
  lr: 0.001

training:
  epochs: 5
  output_dir: ./outputs/config-validate
